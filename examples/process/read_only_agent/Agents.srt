defagent Env {
  meta {
    instance: Singleton
    init_policy: Eager
  }

  @init
  def init() -> Result<String> {
    Ok("demo-home")
  }

  @get
  def get(state: String, key: String) -> Result<String> {
    Ok(key ++ "=" ++ state)
  }
}
