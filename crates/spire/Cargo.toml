[package]
name = "spire"
version = "0.1.0"
edition = "2021"
description = "Surtr's Parser Ingesting Raw Expressions"

[dependencies]
chumsky = { version = "0.12.0", default-features = false, features = ["std"] }
sindr = { path = "../sindr" }
