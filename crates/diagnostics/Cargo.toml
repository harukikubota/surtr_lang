[package]
name = "diagnostics"
version = "0.1.0"
edition = "2021"
description = "Shared Surtr diagnostics rendering utilities"

[dependencies]
ariadne = "0.6.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sindr = { path = "../sindr" }
spire = { path = "../spire" }
