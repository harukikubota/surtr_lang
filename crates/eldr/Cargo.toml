[package]
name = "eldr"
version = "0.1.0"
edition = "2021"
description = "Old Norse for fire — the Surtr virtual machine"

[dependencies]
sindr = { path = "../sindr" }
ariadne = "0.6.0"
regex = "1.11.1"
