[package]
name = "scar"
version = "0.1.0"
edition = "2021"
description = "Surtr's Checker And type Reasoner"

[dependencies]
diagnostics = { path = "../diagnostics" }
spire = { path = "../spire" }
sigil = { path = "../sigil" }
sindr = { path = "../sindr" }
serde = { version = "1.0", features = ["derive"] }
