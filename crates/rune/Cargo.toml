[package]
name = "rune"
version = "0.1.0"
edition = "2021"
description = "Runtime User-facing Node Entry — the Surtr CLI"

[[bin]]
name = "surtr"
path = "src/main.rs"

[dependencies]
spire = { path = "../spire" }
sigil = { path = "../sigil" }
scar  = { path = "../scar" }
forge = { path = "../forge" }
eldr  = { path = "../eldr" }
sindr = { path = "../sindr" }
ariadne = "0.6.0"
serde_json = "1.0"
rustyline = "13"
