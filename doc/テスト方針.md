# Surtr テスト方針（V9）

> 目的: フェーズ名ベースのテスト管理をやめ、仕様ベースのテスト体系へ移行する。

---

## 1. 基本方針

- テストの主語を「実装フェーズ」ではなく「言語機能」にする
- E2E 偏重を解消し、`spec / compile_errors / integration / unit` の 4 層に分割する
- エラー検証は文言完全一致を避け、`phase` と重要断片で判定する

---

## 2. ディレクトリ構成

```
tests/
├── spec/
│   ├── types/
│   ├── control/
│   ├── functions/
│   ├── data/
│   ├── operators/
│   ├── strings/
│   └── errors/
├── compile_errors/
│   ├── type_mismatch/
│   ├── undefined_variable/
│   └── exhaustiveness/
├── integration/
│   ├── run_srt.rs
│   ├── run_eldr.rs
│   ├── build_roundtrip.rs
│   └── repl.rs
└── unit/
    ├── spire/
    ├── sigil/
    ├── scar/
    ├── forge/
    └── eldr/
```

---

## 3. テスト種別

### 3.1 spec/

- 形式: `.srt` + `.expected`
- 判定: `compile -> execute -> stdout` 一致
- 命名: `機能_ケース`（phase 番号を含めない）

例:

- `tests/spec/functions/def_basic.srt`
- `tests/spec/control/match_bool.srt`
- `tests/spec/strings/interpolation_basic.srt`

### 3.2 compile_errors/

- 形式: `.srt` + `.error`
- `.error` には最低 `phase:` と `contains:` を記載

例:

```txt
phase: typecheck
contains: expected Int, got String
```

### 3.3 integration/

- CLI バイナリ経由で入出力契約を検証
- 対象: `run`, `build`, `dump`, `repl`
- `.srt` と `.eldr` 実行のラウンドトリップを含む
- 現在の主な配置: `tests/integration/*.rs`

### 3.4 unit/

- クレート内部 API を直接検証
- 外部 I/O を使わない
- 重点: 境界ケース、網羅性、opcode 列妥当性、VM 不正状態
- 現在の観点整理メモ: `tests/unit/*/README.md`

### 3.5 pending / ignored テスト

- 将来仕様を先に固定したいケースは `#[ignore]` 付きテストとして先置きしてよい
- pending テストには「何待ちで unignore するか」を理由文字列で残す
- 仕様だけ先行し、まだ AST や IR が揃っていないケースで使う

### 3.6 前方参照ポリシー固定テスト

- `def` / `@@builtin def` / `defstruct` / `defrecord` / `deferror` の前方参照成功ケースを `spec` / `integration` で固定する
- 「途中未解決（Pending）だが後段で解決して成功」ケースを追加する
- 全体処理終了時に未解決が残るケースを `compile_errors` で固定する
- 重複トップレベル定義の失敗ケースは既存どおり維持する

### 3.7 標準モジュール境界テスト

- `Bootstrap` / `Kernel` の auto import を `spec` / `integration` で固定する
- `import Bootstrap`, `import Kernel`, `import Kernel::add` のような明示 import 禁止を `compile_errors` で固定する
- 同一 file での module/member 重複 import を `compile_errors` で固定する
- user module / user extension での `@@builtin` 利用禁止を `compile_errors` で固定する
- `Bootstrap -> Kernel -> [他標準モジュール] -> ユーザ拡張` のロード順を `integration` で固定する
- `Bootstrap` source に置いた汎用 error が `Result` / `match` / `eprint` で従来どおり使えることを `spec` で固定する
- step compile の bytecode 完全一致と、将来の並列 compile 向け bytecode 集合同値観点を `unit` の観点として残す

---

## 4. 移行計画

| ステップ | 内容 |
|---|---|
| 1 | 共通テストランナー整備（`.expected` / `.error`） | 完了 |
| 2 | 既存 phase 系テストを `spec/` と `compile_errors/` に分類 | 完了 |
| 3 | CLI テストを `integration/` に集約 | 完了 |
| 4 | eldr unit テストを追加（sindr 抽出後） |
| 5 | spire/sigil/scar/forge の unit を拡充 |
| 6 | phase 命名テストを廃止 |
| 7 | pending / ignored テストを future work の入口として整備 |

---

## 5. 品質ゲート

- `cargo test` が常に通ること
- `spec` は新機能追加時に必ず 1 ケース以上追加
- `compile_errors` は仕様変更時に更新差分を明記
- 新規バグ修正では「再発防止テスト」を同時追加

### 現在の実装状況

- `tests/spec/**.srt + .expected` は導入済み
- `tests/compile_errors/**.srt + .error` は導入済み
- 共通フィクスチャランナーは `tests/integration/run_srt.rs` に配置済み
- `tests/integration/*.rs` は CLI 契約テスト群として配置済み
- 旧 `tests/e2e/**` は廃止済み（`spec/compile_errors` へ移行完了）

---

## 6. 現行からのマッピング例

| 現行名 | 新配置 |
|---|---|
| `phase2_step4_def_minimal` | `spec/functions/def_basic` |
| `step8_match_bool` | `spec/control/match_bool` |
| `phase2_step3_string_interpolation` | `spec/strings/interpolation_basic` |

---

## 7. クレート別テスト観点

### 7.1 sindr

- 観点: `Bytecode` 互換（encode/decode）、`source_map: Option<SourceMap>` 往復、`FunctionEntry` metadata（例: `qualified_name`）互換、`BuiltinMeta` 整合、`Value` 表示規則、`Int=BigInt` の直列化
- CriticalCase: 旧形式 payload（`source_map` なし・関数 metadata 拡張前）decode、標準 module 宣言（`bootstrap.srt` / `kernel.srt`）と `BuiltinMeta` の不一致、未知 tag の表示、tag と user `Int` の混線

### 7.2 spire

- 観点: 優先順位/結合規則、複数行 Incomplete、補間文字列トークナイズ、named args 構文境界、超大整数 tokenization
- CriticalCase: ネスト式の優先順位、補間内構文エラー span、EOF 直前 Incomplete、`BigInt` 化後の負数パターン

### 7.3 sigil

- 観点: shadowing、自由変数 capture、トップレベル宣言事前登録（`def` / `@@builtin def` / `defstruct` / `defrecord` / `deferror`）、`if` 専用ノード化、file 単位 import 状態
- CriticalCase: ネスト closure の capture 解決、`@@builtin` 未定義検出、明示 auto-import 拒否、module/member 重複 import、`unique_id` 決定性、前方参照の保留再解決

### 7.4 scar

- 観点: 型互換、型名/コンストラクタ名のシグネチャ先行登録、`match` 網羅性、`=?` 文脈制約、補間の `Result` 禁止
- CriticalCase: Bool/Result/Int/String の非網羅、`=?` の不正 RHS、関数戻り値不一致、前方参照での型注釈とコンストラクタ呼び出し

### 7.5 forge

- 観点: TypedNode -> Opcode 列、`fun_idx == index` 不変条件、`lexical_captures` / `partial_args` 順序、deprecated opcode 非生成
- CriticalCase: 呼び出し arity、`CaptureClosure` / `CapturePartial` 順序、`Return` 配置、`MakeFrame/PopFrame` 非出力

### 7.6 eldr

- 観点: CallFrame 規約、fail-fast、`fun_idx` 直参照、jump/local/tag 異常系、`Return` 規約
- CriticalCase: top-level `Return`、`LoadLocal` 範囲外、invalid jump、unknown function index、`CallClosure` arity mismatch

### 7.7 rune

- 観点: CLI 契約（run/build/dump/repl）、`.srt/.eldr` roundtrip、dump JSON 互換、REPL 状態継続、標準 module ロード契約
- CriticalCase: build デフォルト出力、dump スキーマ、module 定義関数の `qualified_name` 表示、REPL エラー後状態保持、`:v` 再表示、`Bootstrap -> Kernel -> [他標準モジュール] -> ユーザ拡張` 順序崩れ

---

## 8. CriticalCase 優先順

1. `eldr` 異常系 fail-fast
2. `forge` の `fun_idx == index` と opcode 非退行
3. `sindr` の bytecode 互換（旧 decode）
4. `scar` の網羅性と `=?` 制約
5. `sigil` の capture / `unique_id` 決定性
6. `rune` の roundtrip と REPL 回帰

---

## 9. デグレ事前予防ルール

- バグ修正時は再発ケースを同一 PR に追加する
- 仕様差分は `tests/spec` または `tests/compile_errors` に必ず反映する
- VM/bytecode 変更時は `eldr` 単体と `rune` integration の両方を実行する
- エラー系検証は全文一致ではなく `phase + contains` を使う
- 標準 module 定義（現行実装: `bootstrap.srt` / `kernel.srt`）と実装の整合チェックを CI で常時実行する
- 前方参照ポリシー変更時は、成功系（解決可能）と失敗系（最終未解決）を必ず対で更新する
- 並列化導入後も、ID/tag と診断内容が実行スケジュール非依存であることを回帰テストで保証する

---

*Surtr — 既存の妥協を、型で焼き払う。*
