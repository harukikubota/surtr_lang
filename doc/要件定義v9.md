# Surtr 要件定義書 V9

> V9 の目的は、Phase2 までの実装を言語仕様として確定し、フェーズ3の土台を整えることである。
> 本書は言語仕様と処理系の外部契約を扱い、Rust 実装の詳細はソースコードを正とする。

---

## 1. 言語思想

Surtr は静的型付き関数型言語である。

- コンパイラとランタイムをシンプルに保つ
- 構文の表現力で記述力を高める
- 失敗を型で表現し、曖昧さを早期に排除する

---

## 2. 型システム

### 2.1 基本型

- `Int`
- `Float`（暫定仕様。詳細は `doc/float.md`）
- `String`
- `Boolean`
- `Unit`

### 2.2 合成型

- `List<T>`
- `Result<T>` (`Ok(T)` / `Err(Error)`)
- `Result<T>` をコード上の主表記とし、関数戻り値シグネチャでは `Result<T, E>` を補助的に記述できる
- 関数型 `(T1, T2, ...) -> R`
- ユーザ定義型 (`defstruct`, `defrecord`, `deferror`, `defenum`)

### 2.3 `Option` 相当の扱い

Surtr は独立した `Option<T>` を持たず、欠損値は `Result` に統一する。

- `T?` は `Result<T, NoneError>` の糖衣構文である
- `None` は `Err(NoneError)` として扱う
- `NoneError` は bootstrap で提供される標準エラー値である

### 2.4 エラー型

- `Error` は抽象的な失敗値の型であり、直接構築しない
- `deferror` は `Error` として扱える具象エラー値を定義する
- `Result<T, E>` の `E` には `deferror` により定義された型のみを書ける
- `Err(term)` の `term` には具象エラー値のみを入れ、抽象 `Error` を直接入れない
- `match` は `Result` の `Ok(...)` / `Err(...)` を見るものであり、具象エラー型の分解は提供しない
- `RichError` の確定フィールドは `kind`, `message`, `location`
- `cause` チェーンは V9 範囲外とし、将来拡張で扱う

bootstrap には少なくとも次が含まれる。

```surtr
deferror NoneError { "None Value." }
deferror ZeroDivisionError { "division by zero" }
```

### 2.5 文字列

- 文字列リテラルは二重引用符文字列と単一引用符文字列を持つ
- 二重引用符文字列では式埋め込み `"#{expr}"` をサポートする
- 埋め込み式は `String` 化可能な式に限る

| クォート | 式埋め込み | 転義シーケンス |
|---|---|---|
| `"..."` | あり | `\\`, `\"`, `\'`, `\n`, `\t` |
| `'...'` | なし | `\\`, `\"`, `\'`, `\n`, `\t` |

- `@@doc` 専用に raw の triple-quoted doc string `"""..."""` を持つ
- `@@doc` 内の本文は interpolation しない

---

## 3. 文法

### 3.1 束縛と式

- 束縛は `name = expr`
- 型注釈付き束縛は `name: Ty = expr`
- エラーバインドは `name =? expr`
- セミコロン終端式は `Unit` として扱う

`=?` は SafeBind であり、`Result<T>` 専用ではなく「失敗しうる入力を受けて失敗を伝播する束縛糖衣」として扱う。

- `pattern =? Result<T, E>` の場合:
  - `Ok(value)` のときは `value` を左辺へ束縛して続行する
  - `Err(error)` のときは現在の評価を中断し、失敗をそのまま伝播する
- `pattern =? expr` の場合:
  - `expr` が SafeBind 対象の失敗しうるパターン入力なら、それを左辺パターンへ照合する
  - 現時点の一般化対象は `List` と `Result` に限定する
- トップレベル実行では、その失敗は診断表示の対象になる

`Result<T, E>` は関数戻り値シグネチャでのみ許可し、変数注釈・関数引数・field・関数型内部では `Result<T>` を用いる。

### 3.2 データ定義

- `defstruct Name { field: Ty, ... }`
- `defrecord Name(field: Ty, ...)`
- `deferror Name(field: Ty, ...) { "message template" }`
- `defenum Name { Variant, Variant(Ty), Variant = Int, ... }`
- `impl Type { def method(...) -> ... { ... } }`（`Type` は `defstruct` / `defenum` のみ）

`defenum` の外部契約は次。

- discriminant はデフォルト `0` 起点で自動採番し、明示値の次はその値 + 1 とする
- discriminant は内部管理値であり、surface から直接参照する field accessor は提供しない
- enum に対する `match` は網羅必須
- 循環判定は「全バリアント共通で参照される型」を対象にする

トップレベル宣言の解決規則は以下とする。

- `def` / `@@builtin def` / `@@builtin type` / `defstruct` / `defrecord` / `deferror` / `defenum` / `impl` は同一コンパイル単位で事前登録対象とする
- 参照が定義より前に現れても、最終的に同一コンパイル単位で解決できれば成功とする
- コンパイル途中の未解決参照は保留状態（Pending）として扱い、定義が揃った時点で再解決する
- fixpoint の進捗は Pending 集合の減少で定義する
- 全体処理終了時（または fixpoint 到達時）に未解決が残っている場合のみエラー確定とする
- `impl` は型ごとに 1 ブロックのみ許可する（後付け拡張禁止）
- `impl struct` は `new` の実装を必須とする
- `User(...)` は `User::new(...)` の糖衣として扱う
- `User { ... }` 構造体リテラルは `impl User` の同型メソッド本体内でのみ許可する
- `new` は import 対象外とする

### 3.3 関数

- `def name(args...) -> Ty { ... }`
- `@@builtin def name(args...) -> Ty`
- `@@builtin type Name`
- `@@doc """..."""` は `defmod` / `def` / `deferror` / `defenum` / `@@builtin type` / `@@builtin def` の直前に置ける
- クロージャは `{|x| expr}`
- クロージャ引数型注釈は任意で、`{|x: Int| expr}` 形式を許可する
- 部分適用は `&f`
- qualified capture は `&Type::method`, `&Module::func`, `&Type::method(args...)` を許可する
- FuncLiteral は backtick 付きの parser-only 補助構文とし、`expr \`name\` expr` / `expr \`operator\` expr` を許可する
- FuncLiteral は値ではなく、単独出現・束縛・引数受け渡し・戻り値化・型位置への記述を許可しない
- V1 の FuncLiteral は unqualified name と symbolic operator のみを対象とし、qualified path (`\`Type::method\``) は未対応とする
- V1 では capture 側の operator FuncLiteral (`&\`+\``) と placeholder capture (`&1`) は未実装とし、受理しない
- `type` は予約語とする
- `Self` は `impl` 内の型位置でのみ許可し、対象型へ解決する
- `self` は `impl` メソッドの第一引数専用で、再束縛は禁止する

関数値の扱いは次で固定する。

- 裸の関数参照は許可しない
- 関数値として保持できるのは capture または closure のみ
- `fun_name()` や `Type::method()` は常に関数呼び出しであり、callable marker にはしない
- `>>` / `|=>` は closure value を要求し、call 式は受けない

`@@builtin` の利用境界は次で固定する。

- `@@builtin def` / `@@builtin type` は標準モジュール source（`SourceKind::StdModule`）でのみ許可する
- user script / project module / REPL chunk では `@@builtin` を書けない
- builtin の追加・変更の正本は共有メタデータテーブルであり、Surtr source 側の `@@builtin` 宣言はその宣言層としてのみ扱う
- `@@builtin` は inline (`@@builtin def ...`) と standalone (`@@builtin` の次行に宣言) の両形式を許容する
- builtin type の canonical head は compiler が固定で持ち、標準モジュール source では次と完全一致しなければならない
  - `Int`
  - `Float`
  - `String`
  - `Boolean`
  - `Unit`
  - `Error`
  - `List<$A>`
  - `Result<$T>`

### 3.4 制御構造

- `if_then(flag, (-> Unit)) -> Unit` / `if(flag, (-> A), (-> A)) -> A`
- `cond { cond1 => expr1, ..., True => exprN }`
- `match expr { pattern => expr, ... }`
- `match` は網羅必須

`if` / `if_then` の branch 型が関数型で書かれるのは、選択されなかった branch を
評価しない special form であることを外部契約として明示するためである。
ただし source 上の通常記法では block や closure を明示する必要はなく、
`if(flag, "ok", err_reason)` や `if_then(flag, print("ok"))` のように
通常の式として書ける。

`assert` / `ensure` も guard 用の special form として扱う。

- `assert(Boolean, Error) -> Result<Unit>`
- `ensure(A, (A -> Boolean), Error) -> Result<A>`

外部契約は次で固定する。

- `assert` は条件が `True` のとき `Ok(())` を返し、`False` のときだけ error 引数を評価して `Err(error)` を返す
- `ensure` は値を 1 回だけ評価し、その値を predicate へ渡す
- `ensure` の predicate は unary callable value を要求し、call 式は受けない
- `ensure(value, &pred, err)` と `ensure(value, {|x| ... }, err)` は許可する
- `ensure(value, pred(), err)` のような call 式は不許可とする
- `assert` / `ensure` の error 引数は失敗時にのみ評価する

`cond` は上から順に条件を評価し、最初に `True` になった clause の右辺値を返す。
外部契約は次で固定する。

- 各非終端 clause の左辺は `Boolean` を返す式でなければならない
- 最終 clause は必ずリテラル `True` でなければならない
- `True` clause は最後に 1 回だけ書ける
- すべての clause の右辺型は一致しなければならない
- 選ばれなかった clause の右辺は評価しない

### 3.5 パターン（V9 確定範囲）

- `True`, `False`
- `Ok(x)`, `Err(e)`
- `_`
- `Int` / `String` リテラルパターン
- binding / as-pattern / list pattern / type annotation を `match` arm 左辺でも許可する

`match` は `Result` の成功 / 失敗構造を扱う。具象エラー型での分解は行わない。

`defenum` 導入後の拡張として、`Enum::Variant(...)` 形式の constructor pattern も許可する。

### 3.6 演算子

- 算術: `+ - *`
- 比較: `< <= > >= == !=`
- 文字列結合: `++`
- パイプ / bind:
  - `|>` : 値 apply
  - `|*>` : `Result` / `List` の map apply
  - `|>=` : `Result` / `List` の bind apply
  - `>>` : 通常関数合成
  - `|=>` : `Result` / `List` の Kleisli 合成

パイプ / bind 系の外部契約は次で固定する。

- `|>`, `|*>`, `|>=` は apply 系であり、右辺が call 式なら左辺値を第一引数へ注入する
- `x |> f(1, 2)` は `f(x, 1, 2)` と等価
- `user |> User::get_name()` は `User::get_name(user)` と等価
- `|>` の右辺は capture / closure / call 式を受ける
- `|*>` の右辺は plain function を表す capture / closure / call 式を受ける
- `|>=` の右辺は同一文脈 (`Result` または `List`) を返す capture / closure / call 式を受ける
- `>>` と `|=>` は compose 系であり、左右とも capture または closure でなければならない
- `parse() |=> validate()` や `f() >> g()` のような call 式 compose は不許可
- `|*>`, `|>=`, `|=>` は `Result` と `List` を混在させない
- `|>`, `|*>`, `|>=`, `|=>`, `=?` は同一優先度・左結合で扱う
- `|*>`, `|>=`, `|=>` は最長一致でトークナイズする
- 演算子の結合優先度は `Bind < Apply=Compose < Logical < Expr` とする
- `Expr` クラスの算術演算子 (`+`, `-`, `*`, `++`) は同列・左結合で扱う
- comparison 系 (`==`, `!=`, `<`, `>`, `<=`, `>=`) は `Logical` クラスとし、左結合で扱う
- FuncLiteral の operator 形式は対応する operator のクラスに従い、name 形式は `Expr` クラスで扱う

### 3.7 引数規約

- 名前付き引数は関数・コンストラクタ呼び出しで使用できる
- 位置引数と名前付き引数の混在は禁止する
- `Type::method(...)` をメソッド呼び出しの正規形とする
- `Type(...)` は `impl struct` の `Type::new(...)` 糖衣としてのみ許可する

### 3.8 組込み関数

- `print(String) -> Unit`
- `to_string(A) -> String`
- `inspect(A) -> String`
- `safe_div(A, A) -> Result<A, ZeroDivisionError>` (`Int` / `Float`)
- `safe_mod(Int, Int) -> Result<Int, ZeroDivisionError>`
- `bit_and(Int, Int) -> Int`
- `bit_or(Int, Int) -> Int`
- `bit_xor(Int, Int) -> Int`
- `shl(Int, Int) -> Result<Int, NegativeShiftCount>`
- `shr(Int, Int) -> Result<Int, NegativeShiftCount>`
- `eprint(Error) -> Unit`
- `set_exit_code(Int) -> Unit`
- `assert(Boolean, Error) -> Result<Unit>`
- `ensure(A, (A -> Boolean), Error) -> Result<A>`

`safe_div` / `safe_mod` はゼロ除算時に `Err(ZeroDivisionError)` を返す。
`shl` / `shr` は負 shift count に対して `Err(NegativeShiftCount)` を返す。
`set_exit_code` は `main` 内でのみ使用可能で、評価を継続したままプロセス終了コードを更新する。
`assert` / `ensure` は `Kernel` の special form であり、runtime builtin テーブルではなく
コンパイラ側で遅延評価を伴う分岐へ lower する。

組込み関数と bootstrap 定義のメタデータは共有テーブルまたは事前ロード済み宣言を通して管理し、名前解決・型検査・コード生成・実行が同じ定義を参照する。

---

## 4. 処理系構成

### 4.1 フェーズチェーン

```text
Source -> Ast -> Resolved -> Typed -> Bytecode -> Execute
```

各フェーズはフェーズ固有のエラー型を返す。

### 4.2 コンパイル単位と source 種別

処理系は compile-unit と source-kind を分けて扱う。

- `CompileUnitKind::Script`
  - 単一 script 実行
- `CompileUnitKind::Module`
  - 単体 module source の検証
- `CompileUnitKind::Project`
  - project entrypoint を持つ複数 source compile
- `CompileUnitKind::Repl`
  - REPL の増分入力

- `SourceKind::Script`
  - user script source
- `SourceKind::Module`
  - user module / user extension source
- `SourceKind::StdModule`
  - `Bootstrap` / `Kernel` を含む標準 module source
- `SourceKind::ReplChunk`
  - REPL の 1 入力 chunk

構文許可と `set_exit_code` 制約は `SourceRules` で制御する。

- `Script`
  - top-level expr を許可
  - top-level 宣言は `def` / `import` のみ許可
  - `set_exit_code` はどこでも許可
  - 既定 entrypoint は `main`
- `Module`
  - top-level expr を禁止
  - top-level 宣言は module 定義と型/impl/import 定義のみ許可
  - `@@builtin` を禁止
  - `set_exit_code` は原則禁止
- `StdModule`
  - top-level expr を禁止
  - top-level 宣言は std module 定義と型/impl/import/`@@builtin` 定義のみ許可
  - `@@builtin` を許可
  - `set_exit_code` は原則禁止
- `ReplChunk`
  - top-level expr を許可
  - top-level 宣言は `def` / `import` のみ許可
  - `set_exit_code` を禁止

`CompileUnitKind::Project` では、module / std module に対して `set_exit_code` を entrypoint のみ許可する。

### 4.3 クレート責務

| クレート | 役割 |
|---|---|
| `sindr` | 共通実行基盤型。IR、ランタイム値、組込みメタデータを保持する |
| `spire` | 構文解析 |
| `sigil` | 名前解決 |
| `scar` | 型検査 |
| `forge` | コード生成 |
| `eldr` | Bytecode 実行 |
| `xldr` | REPL と対話実行 |
| `rune` | CLI エントリーポイント |

### 4.4 組込み関数の流れ

- `sigil` は組込み名を解決する
- `scar` は共有メタデータから型シグネチャを参照する
- `forge` は `builtin_id` を使って `CallBuiltin` を生成する
- `eldr` は `builtin_id` に対応する実装を呼び出す
- 共有メタデータの正本は `crates/sindr/src/builtin.rs` の `BUILTIN_METAS` とする

### 4.5 標準モジュールと bootstrap source

- 標準 module の初期ロード順は `Bootstrap -> [Kernel + 他標準モジュール] -> ユーザ拡張` で固定する
- Loader はこの順序を保持し、ユーザ拡張を標準 module より前へ入れない
- `Bootstrap` と `Kernel` は auto import 対象とする
- `Bootstrap` / `Kernel` の明示 `import` は compile error とする
- `Bootstrap` / `Kernel` 以外の標準 module は auto import しない。利用時は明示 `import` する
- import 検査は file 単位で行い、同一 file 内の同一 module / 同一 member の再 import を禁止する

- 標準の先行ロード source は `bootstrap.srt`, `kernel.srt` と type modules に分かれる
- `bootstrap.srt` には `defmod Bootstrap` と、標準 concrete error (`deferror`) を置く
- `kernel.srt` には `defmod Kernel` 配下の cross-cutting builtin 関数と、トップレベルの `Unit` canonical builtin type 宣言を置く
- type ごとの標準モジュールは `int.srt`, `string.srt`, `boolean.srt`, `error.srt`, `list.srt`, `result.srt`, `float.srt` に分ける
- canonical builtin type 宣言は各対応 file のトップレベルに置く
- `@@builtin type` は `defmod` の内側ではなく、標準モジュール file のトップレベル定義として扱う
- `list.srt` はトップレベルに canonical builtin type 宣言 `List<$A>` と `ReduceStep<$A>` を持ち、関数 surface は `defmod List` に置く
- `defmod List` には `cons`, `first`, `last`, `at`, `len`, `append`, `concat`, `reverse`, `reduce`, `reduce_while`, `map`, `flat_map`, `filter`, `find`, `find_map`, `any`, `all` を置く
- `List` は先頭からの逐次処理とパターン分解を中心に扱い、ランダムアクセスや末尾追加は主用途にしない
- Expr 位置の `[head, ..tail]` は list 構築 surface として扱い、Pattern 位置の `[head, ..tail]` は list 分解 surface として扱う。`uncons` を公開関数としては置かない
- `[]` は `List` の Nil であり、singleton 構築は `[x]` または `List::cons(x, [])` で表す
- `List::first([])` は `Err(NoneError)` を返す
- `List::last([])` は `Err(NoneError)` を返す
- `List::at(values, index)` は負 index と範囲外 index に対して `Err(IndexOutOfBounds("index #{index} out of bounds for len #{List::len(values)}"))` を返す
- `List::find`, `List::find_map`, `List::any`, `List::all` の早期終了は `List::reduce_while` と `ReduceStep::Resume` / `ReduceStep::Stop` を基盤にする
- List 構築の推奨スタイルは引き続き `List::cons + List::reverse` とする。公開 surface は `append`, `concat`, `flat_map`, `last`, `at` まで含むが、`zip`, `take`, `drop`, `sort` はまだ含めない
- `int.srt` の `defmod Int` には `safe_div`, `safe_mod`, `bit_and`, `bit_or`, `bit_xor`, `shl`, `shr`, `abs`, `min`, `max`, `sign`, `is_even`, `is_odd` を置き、`bit_and` / `bit_or` / `bit_xor` の direct call は専用 Opcode に lower し、`shl` / `shr` は負 shift count を `Err(NegativeShiftCount(...))` で返す
- `string.srt` の `defmod String` には `is_empty`, `non_empty`, `surround`, `join`, `repeat` を置き、`String::repeat` は負 count で `Err(NegativeRepeatCount(...))` を返す
- `boolean.srt` の `defmod Boolean` には `not`, `xor`, `eqv`, `implies` を置く
- `float.srt` の `defmod Float` には `abs`, `min`, `max` を置く
- 標準モジュールの利用者向け説明は `lib/*.srt` の `@@doc` を正本とする
- `Bootstrap` を先に置く理由は、auto-import の起点と loader の固定基準点を確保するためである
- 現行実装のファイル名は `lib/bootstrap.srt`, `lib/kernel.srt` および各 type module file である

少なくとも bootstrap source には次の汎用 error が含まれる。

```surtr
deferror NoneError { "None Value." }
deferror ZeroDivisionError { "division by zero" }
```

### 4.6 解決と並列化の不変条件

- 段階的ファイルリードを許可し、まず宣言インデックス（名前・種別・シグネチャ・位置）を収集する
- 依存解決は「保留 -> 再試行」を許可し、最終確定は fixpoint 基準で行う
- `unique_id` 採番は並列実行順に依存させず、決定的順序で割り当てる
- 失敗診断（主原因/副原因の位置）は実行スケジュールに依らず決定的であること
- step compile では同一入力から同一 bytecode 列を返す
- 将来の並列 compile では、実行意味に影響しない `func_id` 等の差を許容し、bytecode 集合同値を保証対象とする

---

## 5. ランタイム

### 5.1 実行モデル

- 実行基盤はスタックベース VM
- Bytecode を逐次実行する
- 関数とクロージャ呼び出しをサポートする

### 5.2 Value（概念）

- プリミティブ値
- `List`
- タグ付き値（構造体、レコード、`Result`）
- `Callable`
- `Error`

`inspect` / `to_string` における `Callable` 表示は、bare callable
（`lexical_captures == 0 && partial_args == 0`）かつ runtime metadata から
`module` / `name` / `signature` を復元できる場合、
`FnCapture(module: M, name: f, signature: sig)` を返す。
それ以外の callable は implementation-defined な fallback 表示を使う。

### 5.3 TypeRegistry

- 型タグから型名とフィールド名を逆引きする
- `Ok = 0`, `Err = 1` を予約する
- ユーザ定義型タグは決定的順序で割り当て、並列コンパイル時も同一入力で同じ tag 配置を保証する
- runtime tag は user-visible `Int` とは別の内部 ID として扱う

### 5.4 現行ランタイム表現の暫定方針

- V9 時点では言語機能実装を優先し、プリミティブ値とコレクションの細部挙動は現行 Rust 実装と VM 表現に強く依存してよい
- ここで述べる内容は実装注記であり、将来の最適化や表現変更を妨げる恒久仕様とはしない
- ただし、ユーザに見える挙動へ影響する箇所は本書で明示する

`String` は Rust の UTF-8 文字列として保持する。

- 長さ、走査、部分操作のコストは UTF-8 と実装方式に依存する
- バイト数、Unicode scalar value 数、表示上の文字数は一致しない場合がある
- V9 では `==`, `!=`, `++`, 表示系 builtin を優先し、細粒度の文字列 API は慎重に追加する

`Int` は意味論上 unbounded integer として扱う。

- 現行実装は `BigInt` を用いる
- 算術オーバーフローは `Int` の通常計算では起きない
- `set_exit_code` や bit shift のようにホスト整数へ落とす箇所では、明示的な範囲検査を行う

`Float` は Rust の `f64` 相当として扱う。

- NaN, Infinity, `-0.0` を含む挙動は当面 Rust に準拠する
- 比較、表示、丸めの細部は将来調整される可能性がある
- 厳密な数値意味論の固定より、言語機能の整備を優先する
- `Float` の継続論点は `doc/float.md` で管理する

`UnsignedInt` は V9 範囲外とする。

- 導入時は Rust の対応整数型に準拠して実装してよい
- ビット幅、オーバーフロー、シフト、比較規則は導入時に明文化する
- `Int` と `UnsignedInt` の相互演算規則は未定義のまま先行実装しない

`List` は VM 上で cons cell 表現を前提とする。

- 先頭追加と分解は自然だが、ランダムアクセスや末尾追加は効率的ではない
- リスト操作の性能特性は配列系コンテナとは異なる
- V9 では関数型リストとしての意味論を優先し、高速シーケンス処理は将来別表現または別型で補いうる

---

## 6. ツール

### 6.1 Rune CLI

- `surtr run <file.srt|file.eldr>`
- `surtr build <file.srt> [output.eldr]`
- `surtr dump <file.eldr> [--format json]`
- `surtr repl`

### 6.2 Xldr

- `surtr repl` の対話実行本体は `xldr` が担う
- REPL と TUI ステッパーの外部契約は `doc/Xldr_spec.md` を参照する

### 6.3 エラー出力

- 人間向け診断は ariadne 形式を用いる
- 機械向け診断は JSON を用いる

---

## 7. V9 範囲外

- トレイト
- 型エイリアス / NewType
- パイプライン `|>`
- `result do`
- マクロシステム拡張
- 複数モジュール
- 並列コンパイル
- プリコンパイル成果物（`.srtc`）

---

*Surtr — 既存の妥協を、型で焼き払う。*
