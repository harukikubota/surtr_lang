# Surtr 要件定義書 V9

> V9 の目的は、Phase2 までの実装を言語仕様として確定し、フェーズ3の土台を整えることである。
> 本書は言語仕様と処理系の外部契約を扱い、Rust 実装の詳細はソースコードを正とする。

---

## 1. 言語思想

Surtr は静的型付き関数型言語である。

- コンパイラとランタイムをシンプルに保つ
- 構文の表現力で記述力を高める
- 失敗を型で表現し、曖昧さを早期に排除する

---

## 2. 型システム

### 2.1 基本型

- `Int`
- `Float`
- `String`
- `Boolean`
- `Unit`

### 2.2 合成型

- `List<T>`
- `Result<T>` (`Ok(T)` / `Err(Error)`)
- 関数戻り値シグネチャでは `Result<T, E>` を記述できる
- 関数型 `(T1, T2, ...) -> R`
- ユーザ定義型 (`defstruct`, `defrecord`, `deferror`)

### 2.3 `Option` 相当の扱い

Surtr は独立した `Option<T>` を持たず、欠損値は `Result` に統一する。

- `T?` は `Result<T, NoneError>` の糖衣構文である
- `None` は `Err(NoneError)` として扱う
- `NoneError` は bootstrap で提供される標準エラー値である

### 2.4 エラー型

- `Error` は抽象的な失敗値の型であり、直接構築しない
- `deferror` は `Error` として扱える具象エラー値を定義する
- `Result<T, E>` の `E` には `deferror` により定義された型のみを書ける
- `RichError` の確定フィールドは `kind`, `message`, `location`
- `cause` チェーンは V9 範囲外とし、将来拡張で扱う

bootstrap には少なくとも次が含まれる。

```surtr
deferror NoneError { "None Value." }
deferror ZeroDivisionError { "division by zero" }
```

### 2.5 文字列

- 文字列リテラルは二重引用符文字列と単一引用符文字列を持つ
- 二重引用符文字列では式埋め込み `"#{expr}"` をサポートする
- 埋め込み式は `String` 化可能な式に限る

| クォート | 式埋め込み | 転義シーケンス |
|---|---|---|
| `"..."` | あり | `\\`, `\"`, `\n`, `\t` |
| `'...'` | なし | `\\`, `\'` |

---

## 3. 文法

### 3.1 束縛と式

- 束縛は `name = expr`
- 型注釈付き束縛は `name: Ty = expr`
- エラーバインドは `name =? expr`
- セミコロン終端式は `Unit` として扱う

`=?` は `Result<T>` を受け取る束縛糖衣である。

- `Ok(value)` のときは `value` を左辺へ束縛して続行する
- `Err(error)` のときは現在の評価を中断し、失敗をそのまま伝播する
- トップレベル実行では、その失敗は診断表示の対象になる

`Result<T, E>` は関数戻り値シグネチャでのみ許可し、変数注釈・関数引数・field・関数型内部では `Result<T>` を用いる。

### 3.2 データ定義

- `defstruct Name { field: Ty, ... }`
- `defrecord Name(field: Ty, ...)`
- `deferror Name(field: Ty, ...) { "message template" }`

トップレベル宣言の解決規則は以下とする。

- `def` / `@@builtin def` / `defstruct` / `defrecord` / `deferror` は同一コンパイル単位で事前登録対象とする
- 参照が定義より前に現れても、最終的に同一コンパイル単位で解決できれば成功とする
- コンパイル途中の未解決参照は保留状態（Pending）として扱い、定義が揃った時点で再解決する
- 全体処理終了時（または fixpoint 到達時）に未解決が残っている場合のみエラー確定とする

### 3.3 関数

- `def name(args...) -> Ty { ... }`
- `@@builtin def name(args...) -> Ty`
- クロージャは `{|x| expr}`
- 部分適用は `&f`

`@@builtin` の利用境界は次で固定する。

- `@@builtin def` は標準モジュール source（`SourceKind::StdModule`）でのみ許可する
- user script / project module / REPL chunk では `@@builtin` を書けない
- builtin の追加・変更の正本は共有メタデータテーブルであり、Surtr source 側の `@@builtin` 宣言はその宣言層としてのみ扱う

### 3.4 制御構造

- `if_then(cond, (-> Unit)) -> Unit` / `if(cond, (-> A), (-> A)) -> A`
- `match expr { pattern => expr, ... }`
- `match` は網羅必須

### 3.5 パターン（V9 確定範囲）

- `True`, `False`
- `Ok(x)`, `Err(e)`
- `_`
- `Int` / `String` リテラルパターン

`match` は `Result` の成功 / 失敗構造を扱う。具象エラー型での分解は行わない。

### 3.6 演算子

- 算術: `+ - *`
- 比較: `< <= > >= == !=`
- 文字列結合: `++`

### 3.7 組込み関数

- `print(String) -> Unit`
- `to_string(A) -> String`
- `inspect(A) -> String`
- `safe_div(A, A) -> Result<A>` (`Int` / `Float`)
- `safe_mod(Int, Int) -> Result<Int>`
- `eprint(Error) -> Unit`
- `set_exit_code(Int) -> Unit`

`safe_div` / `safe_mod` はゼロ除算時に `Err(ZeroDivisionError)` を返す。
`set_exit_code` は `main` 内でのみ使用可能で、評価を継続したままプロセス終了コードを更新する。

組込み関数と bootstrap 定義のメタデータは共有テーブルまたは事前ロード済み宣言を通して管理し、名前解決・型検査・コード生成・実行が同じ定義を参照する。

---

## 4. 処理系構成

### 4.1 フェーズチェーン

```text
Source -> Ast -> Resolved -> Typed -> Bytecode -> Execute
```

各フェーズはフェーズ固有のエラー型を返す。

### 4.2 コンパイル単位と source 種別

処理系は compile-unit と source-kind を分けて扱う。

- `CompileUnitKind::Script`
  - 単一 script 実行
- `CompileUnitKind::Module`
  - 単体 module source の検証
- `CompileUnitKind::Project`
  - project entrypoint を持つ複数 source compile
- `CompileUnitKind::Repl`
  - REPL の増分入力

- `SourceKind::Script`
  - user script source
- `SourceKind::Module`
  - user module / user extension source
- `SourceKind::StdModule`
  - `Bootstrap` / `Kernel` を含む標準 module source
- `SourceKind::ReplChunk`
  - REPL の 1 入力 chunk

構文許可と `set_exit_code` 制約は `SourceRules` で制御する。

- `Script`
  - top-level expr を許可
  - `set_exit_code` はどこでも許可
  - 既定 entrypoint は `main`
- `Module`
  - top-level expr を禁止
  - `@@builtin` を禁止
  - `set_exit_code` は原則禁止
- `StdModule`
  - top-level expr を禁止
  - `@@builtin` を許可
  - `set_exit_code` は原則禁止
- `ReplChunk`
  - top-level expr を許可
  - `set_exit_code` を禁止

`CompileUnitKind::Project` では、module / std module に対して `set_exit_code` を entrypoint のみ許可する。

### 4.3 クレート責務

| クレート | 役割 |
|---|---|
| `sindr` | 共通実行基盤型。IR、ランタイム値、組込みメタデータを保持する |
| `spire` | 構文解析 |
| `sigil` | 名前解決 |
| `scar` | 型検査 |
| `forge` | コード生成 |
| `eldr` | Bytecode 実行 |
| `xldr` | REPL と対話実行 |
| `rune` | CLI エントリーポイント |

### 4.4 組込み関数の流れ

- `sigil` は組込み名を解決する
- `scar` は共有メタデータから型シグネチャを参照する
- `forge` は `builtin_id` を使って `CallBuiltin` を生成する
- `eldr` は `builtin_id` に対応する実装を呼び出す

### 4.5 標準モジュールと bootstrap source

- 標準 module の初期ロード順は `Bootstrap -> Kernel -> [他標準モジュール] -> ユーザ拡張` で固定する
- Loader はこの順序を保持し、ユーザ拡張を標準 module より前へ入れない
- `Bootstrap` と `Kernel` は auto import 対象とする
- `Bootstrap` / `Kernel` の明示 `import` は compile error とする
- `Bootstrap` / `Kernel` 以外の標準 module は auto import しない。利用時は明示 `import` する
- import 検査は file 単位で行い、同一 file 内の同一 module / 同一 member の再 import を禁止する

- 標準の先行ロード source は `bootstrap.srt` と `kernel.srt` に分かれる
- `bootstrap.srt` には `defmod Bootstrap` と、builtin 先行解決層に属する汎用 error 定義を置く
- `kernel.srt` には `defmod Kernel` と、builtin 以外の標準 API を置く
- `Bootstrap` を `Kernel` より先に置く理由は、builtin 宣言と汎用 error を先に解決し、後段 module の待機を減らすためである
- 現行実装のファイル名は `lib/bootstrap.srt` と `lib/kernel.srt` である

少なくとも bootstrap source には次の汎用 error が含まれる。

```surtr
deferror NoneError { "None Value." }
deferror ZeroDivisionError { "division by zero" }
```

### 4.6 解決と並列化の不変条件

- 段階的ファイルリードを許可し、まず宣言インデックス（名前・種別・シグネチャ・位置）を収集する
- 依存解決は「保留 -> 再試行」を許可し、最終確定は fixpoint 基準で行う
- `unique_id` 採番は並列実行順に依存させず、決定的順序で割り当てる
- 失敗診断（主原因/副原因の位置）は実行スケジュールに依らず決定的であること
- step compile では同一入力から同一 bytecode 列を返す
- 将来の並列 compile では、実行意味に影響しない `func_id` 等の差を許容し、bytecode 集合同値を保証対象とする

---

## 5. ランタイム

### 5.1 実行モデル

- 実行基盤はスタックベース VM
- Bytecode を逐次実行する
- 関数とクロージャ呼び出しをサポートする

### 5.2 Value（概念）

- プリミティブ値
- `List`
- タグ付き値（構造体、レコード、`Result`）
- `Callable`
- `Error`

### 5.3 TypeRegistry

- 型タグから型名とフィールド名を逆引きする
- `Ok = 0`, `Err = 1` を予約する
- ユーザ定義型タグは決定的順序で割り当て、並列コンパイル時も同一入力で同じ tag 配置を保証する

### 5.4 現行ランタイム表現の暫定方針

- V9 時点では言語機能実装を優先し、プリミティブ値とコレクションの細部挙動は現行 Rust 実装と VM 表現に強く依存してよい
- ここで述べる内容は実装注記であり、将来の最適化や表現変更を妨げる恒久仕様とはしない
- ただし、ユーザに見える挙動へ影響する箇所は本書で明示する

`String` は Rust の UTF-8 文字列として保持する。

- 長さ、走査、部分操作のコストは UTF-8 と実装方式に依存する
- バイト数、Unicode scalar value 数、表示上の文字数は一致しない場合がある
- V9 では `==`, `!=`, `++`, 表示系 builtin を優先し、細粒度の文字列 API は慎重に追加する

`Float` は Rust の `f64` 相当として扱う。

- NaN, Infinity, `-0.0` を含む挙動は当面 Rust に準拠する
- 比較、表示、丸めの細部は将来調整される可能性がある
- 厳密な数値意味論の固定より、言語機能の整備を優先する

`UnsignedInt` は V9 範囲外とする。

- 導入時は Rust の対応整数型に準拠して実装してよい
- ビット幅、オーバーフロー、シフト、比較規則は導入時に明文化する
- `Int` と `UnsignedInt` の相互演算規則は未定義のまま先行実装しない

`List` は VM 上で cons cell 表現を前提とする。

- 先頭追加と分解は自然だが、ランダムアクセスや末尾追加は効率的ではない
- リスト操作の性能特性は配列系コンテナとは異なる
- V9 では関数型リストとしての意味論を優先し、高速シーケンス処理は将来別表現または別型で補いうる

---

## 6. ツール

### 6.1 Rune CLI

- `surtr run <file.srt|file.eldr>`
- `surtr build <file.srt> [output.eldr]`
- `surtr dump <file.eldr> [--format json]`
- `surtr repl`

### 6.2 Xldr

- `surtr repl` の対話実行本体は `xldr` が担う
- REPL と TUI ステッパーの外部契約は `doc/Xldr_spec.md` を参照する

### 6.3 エラー出力

- 人間向け診断は ariadne 形式を用いる
- 機械向け診断は JSON を用いる

---

## 7. V9 範囲外

- トレイト
- 型エイリアス / NewType
- パイプライン `|>`
- `result do`
- マクロシステム拡張
- 複数モジュール
- 並列コンパイル
- プリコンパイル成果物（`.srtc`）

---

*Surtr — 既存の妥協を、型で焼き払う。*
