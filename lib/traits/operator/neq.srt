@doc """
Standard `Neq` operator trait declaration.
`Neq` models the `!=` / `neq(...)` contract.

## REPL
xldr(1)> neq(1, 2)
True
"""
@autoimport
deftrait Neq {
  # Return `True` when both values are not equal.
  def neq(self: Self, rhs: Self) -> Boolean
}
