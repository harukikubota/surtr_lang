@doc """
Standard `Gt` operator trait declaration.
`Gt` models the `>` / `gt(...)` contract.

## REPL
xldr(1)> gt(2, 1)
True
"""
@autoimport
deftrait Gt {
  # Return `True` when the left value is strictly greater.
  def gt(self: Self, rhs: Self) -> Boolean
}
