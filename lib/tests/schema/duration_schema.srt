@derive Compare
defstruct Timeout {
  value: Duration,
}

impl Show for Timeout {
  def to_string(self: Self) -> String {
    inspect(self.value)
  }
}

impl Timeout {
  def new(value: Duration) -> Self {
    Timeout { value: value }
  }
}
