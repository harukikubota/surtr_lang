@@doc """
Standard `Show` trait declaration.
`Show` is the user-facing textual conversion contract.
Standard runtime-visible values may use compiler-provided implementations,
while user-defined named types can provide explicit impls.
"""
@@autoimport
deftrait Show {
  def to_string(self: Self) -> String
}
