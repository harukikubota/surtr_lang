@@doc """
Standard `Concat` trait declaration.
`Concat` models the `++` / `concat(...)` contract.
The unqualified `concat(...)` helper comes from this auto-imported trait
surface, while concrete `String` dispatch may still lower to an optimized
runtime opcode.
"""
@@autoimport
deftrait Concat {
  def concat(self: Self, rhs: Self) -> Self
}
