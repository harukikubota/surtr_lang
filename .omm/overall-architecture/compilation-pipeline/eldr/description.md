The virtual machine crate. `eldr` executes bytecode, manages values and call frames, dispatches builtins by builtin id, and distinguishes runtime corruption from language-level `Result` error values.
