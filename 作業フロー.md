# Surtr 改善作業フロー

最終更新: 2026-04-08

## 目的

`doc/改善.md` に列挙されたレビュー・設計メモ・仕様差分を、実装順とコミット単位に分解する。

今回の整理では、次の 3 点を先に固定する。

- ユーザー向け整数 `Int` は `BigInt` を採用し、オーバーフローを言語仕様から外す
- ランタイム内部 ID は `u32` / `u16` のまま固定し、ユーザー向け `Int` と分離する
- `Float` は現行実装を維持しつつ、正本仕様を別ファイルへ切り出して暫定扱いにする

## 判断メモ

- `Float` は今回の作業では削除しない
  現行コードとテスト資産の影響が大きいため、まずは `doc/float.md` に切り出して「暫定実装 / 今後仕様確定」とする。
- `@@builtin` と標準型宣言の統合は今回の実装対象を限定する
  `@@builtin` 単独行と `def` の整合は現行系を維持し、`@@builtin` + `type` 宣言読み取りは pending テストを先に置く。
- 破壊的変更は許容するが、変更はコミット単位で追えるように分割する
- 空の `def` と空の `match` は今回 compile error として固定する
- 将来仕様へ寄せるための破壊的変更は許容し、互換レイヤー維持を優先しない

## 今回の前提として固定するもの

- 空の `def` は compile error
  parser または直後の validate 段階で止め、空 block を「意味未確定の仮置き」として温存しない。
- 空の `match` は compile error
  Phase 1 では網羅性が必須なので、空 arm を後段へ流さず早期に止める。
- 将来に向けた大胆な破壊的変更を許容する
  旧 API や旧 AST に合わせた互換維持より、構文追加・ルール追加・型表現整理を優先してよい。
- `AstTy::Generic` を導入する
  `List<$A>` を含む generic 型注釈を AST で保持し、型引数を silent drop しない。
- match arm の LHS は `SafeBind.LHS` と同じ構文に統一する
  先頭 binding は catch-all として許可し、ドキュメントもこの前提へ揃える。
- single quote と double quote の escape は対称にする
  差分は embedded / interpolated string を展開するかどうかだけに固定する。
- closure 引数型注釈は任意にする
  期待型があればそれで推論し、注釈があれば整合検証する。closure ごとの推論状態は外側へ mutable に共有しない。
- substitution 伝播は「子 checker が成功したときだけ親へ compose する」に固定する
- declaration index の初期収集情報は `name / kind / signature / span / module path` に固定する
- fixpoint の進捗定義は Pending 集合の減少にする
- `@@builtin` の次に `type` 宣言が続く form は今回の surface syntax として受理する
- current phase の型循環は一律禁止にする
  現状対象の struct / record / error / type 相当では循環を compile error にする。
- 実行予算を将来導入する場合の基本方式は fuel にする
- runtime error taxonomy は
  回復不能な失敗を runtime error、ユーザ制御可能な失敗を `Result`、プロセス由来の失敗を Supervisor 吸収とする方向で文書化する

## 現時点で Issue へ残し、今回の確認対象から外すもの

- `Float`
  `doc/float.md` と関連 Issue に残し、`NaN` / `Infinity` / `-0.0` / 非有限値 API 契約は今回ここで確定しない。
- project runner
  source 操作 API、runner DSL、init command は `surtr_stdmodv1_issue_reorg.md` の future issue として扱い、今回の確認対象から外す。
- REPL command
  `:load` / `:type` / `:browse` などの command set と UX は future issue として残し、今回の確認対象から外す。
- macro
  展開段階、`unique_id` / `tag` の割り当て、通常解決との分離は `open-issues.md` の macro 論点として据え置く。
- closure の `expected=None` 推論強化
  期待型なしでも強く推論する方向は今回やらず、Issue 側へ移す。
- OOM 詳細
  上限値や host 差異の吸収は今回やらず、host 依存の停止理由として Issue 側へ残す。

## `doc/改善.md` 対応マトリクス

- `doc/eldr_review.md`
  - 対応: Commit 1, 2, 3, 4
  - 内容: `BigInt` 化、runtime tag 分離、`Float` 論点の別紙化、未カバーテストの一部は pending 化
- `doc/EldrVM_spec.md`
  - 対応: Commit 4
  - 内容: `Int=BigInt`、`Tag(u32)` 分離、`Float` の暫定仕様反映
- `doc/open-issues.md` の「マクロ以外で解決できそうなところ」
  - 対応: Commit 6
  - 内容: 既に正本へ取り込めるものと、本当に未解決のものを仕分ける
- `doc/project_src_review.txt`
  - 対応: Commit 1
  - 内容: 今回着手対象と deferred 項目を明示する
  - 未処理で残す棚卸し: `push_atomic` clone コスト、診断重複、読み取り API の clone 削減
- `doc/scar-review.md`
  - 対応: Commit 1
  - 内容: 今回着手対象と future bug を仕分ける
- `doc/spire_review_2026-04-06.md`
  - 対応: Commit 1, 3, 5
  - 内容: `i64` 前提の項目を `BigInt` 移行へ寄せ、将来の parser work は pending/future 化
- `doc/surtr_stdmodv1_followup_tasks.md` の未実装洗い出し
  - 対応: Commit 6
  - 内容: StdModV1 固有の未実装を「今回の改善対象」「将来 backlog」に再配置する
- `doc/surtr_stdmodv1_issue_reorg.md` の未実装洗い出し
  - 対応: Commit 6
  - 内容: 実施済み Issue と future issue の境界を、今回の改善作業と衝突しない形で再整理する
- `doc/test_runner_design.md` の未実装洗い出し
  - 対応: Commit 5
  - 内容: pending / ignored テストの先置きと、`@@builtin` 共存ケースの future coverage 化
- `doc/surtr_runtime_failure_policy.md`
  - 対応: Commit 4
  - 内容: `BigInt` と runtime 内部 ID 分離、`Float` 継続論点の反映
- `doc/surtr_builtin_type_resolution_design.md`
  - 対応: Commit 5, 6
  - 内容: `@@builtin` + `type` の将来構文を pending テストと future design に接続する
- `doc/要件定義v9.md`, `doc/テスト方針.md`, `doc/EldrVM_spec.md`, `README.md`, `AGENTS.md`
  - 対応: Commit 4
  - 内容: 正本更新
- `doc/surtr_operator_impl_memo.md`
  - 対応: future only
  - 内容: 今回は実装しないが、将来着手対象として位置づけを固定する

## 残件の分類

この章では、`doc/改善.md` から辿る各レビュー / 設計メモのうち、まだ未処理のものを
「確認不要でタスク化できるもの」と「仕様判断が先に必要なもの」に分ける。

すでに今回の整理で取り込み済みの `Int=BigInt`、runtime 内部 ID 分離、`Float` 別紙化は
既存の commit 計画側で追うため、この章では残件だけを扱う。

## 確認不要でタスクへ落とせる項目

### `spire`

- `AstTy::Generic` を導入し、既存の `ListOf` / `ResultOf` special case を generic 表現へ置き換える。
- `List<$A>`、`Result<T, E>`、将来 generic の parser 保持を行い、型引数を silent drop しない。
- match arm の LHS を `SafeBind.LHS` 相当へ統一する。
  binding / wildcard / literal / list / constructor / as-pattern / annotation を同じ grammar で受理する。
- `Token::Unit` を match pattern で受理する。
- 空の `def` と空の `match` を compile error にする。
- single quote と double quote の escape を対称化する。
- closure 引数型注釈を任意で受理する。
- `\#{...}` の補間エスケープ時に `\` を残さない。
- 補間式内改行や未対応のクロージャ引数型注釈について、ユーザー向けに理由が分かる診断へ整える。
- `match` pattern 制限エラーから開発者向け文言を分離する。
- Lexer / parser の unit test を拡充する。
  `DotDot`、コメント、空入力、コメント only source、比較演算子、`import Mod::{}`、深い path import、`Ok()` / `Ok(a, b)` の誤用、`StructDef` / `StructLit` / `RecordDef` / `ConstructorCall` / `DeferrorDef` / `FieldAccess` / path call、`@@builtin + type`、closure param annotation、有効/無効な match pattern を固定する。

### `scar`

- `Binding` パターンを exhaustive 扱いにする。
- struct literal の余剰フィールドを型エラー化する。
- constructor call の named args 重複を型エラー化する。
- `Eq` / `Neq` のエラーパスで substitution を汚染しないようにする。
- closure / arm / child scope の substitution はローカルに持ち、成功時のみ親へ compose する。
- Binding パターンで束縛した型は env 登録時と arm 完了時に再正規化し、未解決 tyvar を後段へ漏らさない。
- closure 引数型注釈があれば expected param と照合し、なければ expected から推論する。
- `deferror` の `show_expr` で元の span を保持する。
- 上記 bug と現在の Phase 1 相当仕様に対する回帰テストを追加する。

### `eldr`

- `vm.rs` の未カバー正常系 opcode の unit test を追加する。
- `vm.rs` の未カバー error path を unit test で埋める。
- `builtin.rs` の型不一致 / 範囲超過 / `eprint` 分岐の unit test を追加する。
- `error.rs` の `report_runtime_error` と `runtime_error_verbose_enabled` の test を追加する。

### `rune` / `xldr` / 共通基盤

- diagnostics の重複実装を共通化する。
  共有先を crate にするか shared module にするかは実装時裁量でよいが、診断ロジック本体は一本化する。
- `report_error` の `unwrap` を除去し、描画失敗時のフォールバック経路を持たせる。
- `TypeRegistry` getter を読み取り参照ベースへ寄せ、clone 前提の呼び出し箇所を減らす。
- `push_atomic` は VM 全体 clone から checkpoint + append/rollback 方式へ寄せる。
  少なくとも stack / frames / constants / functions / source map の長さベース rollback で REPL hot path のコピーを減らす。
- `with_child_scope` の `next_id` 反映意図、`expect` / `unwrap` が内部不変条件に依存している箇所にコメントや `debug_assert!` を補う。

### `test_runner` / `stdmod` / 文書整理

- `doc/test_runner_design.md` の確定済み仕様に従い、`TST-001` から `TST-010` までをそのまま backlog 化する。
- `@@builtin` / `@@test` 関連の pending / ignored テストを先置きし、理由をテスト名または ignore 理由に残す。
- `doc/surtr_stdmodv1_followup_tasks.md` のうち、旧 API 移行残件、標準 module 境界テスト、決定性 / dump 回帰観点を個別タスクへ分解する。
- `doc/open-issues.md` は今回確定した `OI-001`〜`OI-004` の前提を反映し、未確定論点だけを残す整理に更新する。
- `doc/surtr_builtin_type_resolution_design.md` は
  「型名はコード上の宣言として受理し、builtin semantics の中身は compiler が持つ」という境界へ書き換える。
- `doc/surtr_stdmodv1_issue_reorg.md` は script 非関数トップレベル定義を future issue として維持し、今回確定しないことを明示する。
- `doc/要件定義v9.md`、`doc/テスト方針.md`、`doc/EldrVM_spec.md`、`README.md`、`AGENTS.md` を今回の確定仕様へ追従させる。

## コミット単位タスク

### Commit 1: `docs: consolidate improvement scope and decision records`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/作業フロー.md`
  - `/Users/haruca/work/rust/surtr/doc/改善.md`
  - `/Users/haruca/work/rust/surtr/doc/eldr_review.md`
  - `/Users/haruca/work/rust/surtr/doc/project_src_review.txt`
  - `/Users/haruca/work/rust/surtr/doc/scar-review.md`
  - `/Users/haruca/work/rust/surtr/doc/spire_review_2026-04-06.md`
- 受け入れ条件:
  - レビュー由来の修正項目が「今回確定」「確認不要でタスク化」「Issueへ退避」「pending テスト」「将来実装」に分類されている
  - `BigInt`、内部 ID 分離、`Float` 切り出しの判断が明文化されている
  - 今後の実装順が 1 ファイルで追える
- テストケース:
  - ドキュメント変更のみのため自動テストなし

### Commit 2: `runtime: introduce shared primitives and separate internal ids`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/Cargo.toml`
  - `/Users/haruca/work/rust/surtr/crates/sindr/src/primitives.rs`
  - `/Users/haruca/work/rust/surtr/crates/sindr/src/lib.rs`
  - `/Users/haruca/work/rust/surtr/crates/sindr/src/ir.rs`
  - `/Users/haruca/work/rust/surtr/crates/sindr/src/runtime.rs`
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/vm.rs`
  - `/Users/haruca/work/rust/surtr/crates/forge/src/codegen.rs`
- 受け入れ条件:
  - ユーザー向け整数型と内部 tag/builtin/function ID が型レベルで分離される
  - `Result` tag や構造体 tag の生成・比較で user `Int` を流用しない
  - VM のタグ比較が `Int` 比較と独立する
- テストケース:
  - `cargo test -p sindr`
  - `cargo test -p eldr`
  - `cargo test -p forge`

### Commit 3: `int: migrate Surtr Int to BigInt end-to-end`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/spire/src/token.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/ast.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/lexer.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/parser.rs`
  - `/Users/haruca/work/rust/surtr/crates/sigil/src/resolved.rs`
  - `/Users/haruca/work/rust/surtr/crates/scar/src/typed.rs`
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/builtin.rs`
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/vm.rs`
  - 関連ユニットテスト一式
- 受け入れ条件:
  - 超大整数を lex/parse/typecheck/execute まで通せる
  - `-i64::MIN` 由来のオーバーフロー経路が消える
  - `set_exit_code` / `shl` / `shr` は `BigInt` から安全にホスト整数へ落とす
- テストケース:
  - `cargo test -p spire`
  - `cargo test -p sigil`
  - `cargo test -p scar`
  - `cargo test -p eldr`
  - `cargo test -p rune --test language_features`

### Commit 4: `spire: introduce generic type AST and parser updates`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/spire/src/ast.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/token.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/lexer.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/parser.rs`
  - 関連 parser / lexer unit test
- 受け入れ条件:
  - `AstTy::Generic(Span, Symbol, Vec<AstTy>)` が導入され、`ListOf` / `ResultOf` special case が置き換わる
  - `List<$A>`、`Result<T, E>`、`@@builtin + type`、closure param annotation を parser が保持できる
  - 空の `def` / `match` が compile error になる
  - single quote と double quote の escape が対称化される
  - `\#{...}` を文字通り出したいケースで `\` が残らない
- テストケース:
  - `cargo test -p spire`

### Commit 5: `spire: unify match arm lhs with safe-bind patterns`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/spire/src/ast.rs`
  - `/Users/haruca/work/rust/surtr/crates/spire/src/parser.rs`
  - `/Users/haruca/work/rust/surtr/crates/sigil/src/resolved.rs`
  - `/Users/haruca/work/rust/surtr/crates/sigil/src/resolver.rs`
  - 関連 unit test
- 受け入れ条件:
  - match arm の LHS が `SafeBind.LHS` と同じ構文集合を受理する
  - 先頭 binding が catch-all として解決される
  - list / constructor / as-pattern / annotated pattern の match arm が end-to-end で解決される
  - 旧 `AstMatchPattern` / `ResolvedMatchPattern` 前提のコードが新しい pattern 表現へ寄る
- テストケース:
  - `cargo test -p spire`
  - `cargo test -p sigil`

### Commit 6: `scar: rebase generic annotations and pattern typing`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/scar/src/checker.rs`
  - `/Users/haruca/work/rust/surtr/crates/scar/src/typed.rs`
  - 必要なら `/Users/haruca/work/rust/surtr/crates/scar/src/env.rs`
  - 関連 unit test
- 受け入れ条件:
  - `AstTy::Generic` を type annotation 解決に反映できる
  - current phase の型循環が struct / record / error / type 相当で compile error になる
  - `Binding` パターンが exhaustive と認識される
  - struct literal 余剰フィールド、constructor named args 重複、`Eq/Neq` substitution 汚染、`deferror` span が修正される
  - Binding パターンで束縛した型は env 登録時と arm 完了時に再正規化され、未解決 tyvar が後段へ漏れない
- テストケース:
  - `cargo test -p scar`

### Commit 7: `scar: localize substitution flow for closures and child scopes`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/scar/src/checker.rs`
  - 関連 unit test
- 受け入れ条件:
  - closure / arm / child scope の substitution はローカルに保持され、成功時のみ親へ compose される
  - closure param annotation が optional に扱われ、expected param と整合検証できる
  - closure ごとの推論状態が外側 checker を mutable に汚さない
  - `expected=None` 強化は行わず、未対応ケースは既存制限または pending test に整理される
- テストケース:
  - `cargo test -p scar`

### Commit 8: `runtime: reduce repl clone cost and cleanup diagnostics api`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/vm.rs`
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/builtin.rs`
  - `/Users/haruca/work/rust/surtr/crates/rune/src/diagnostics.rs`
  - `/Users/haruca/work/rust/surtr/crates/xldr/src/diagnostics.rs`
  - 必要なら共通 diagnostics 置き場
  - 関連 unit / integration test
- 受け入れ条件:
  - `push_atomic` が VM 全体 clone ではなく checkpoint + append/rollback ベースになる
  - `TypeRegistry` 読み取り API が参照ベースに寄る
  - diagnostics 実装本体が `rune` / `xldr` で共通化される
  - `report_error` の描画失敗で panic しない
- テストケース:
  - `cargo test -p eldr`
  - `cargo test -p rune`

### Commit 9: `runtime: expand vm and builtin regression coverage`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/vm.rs`
  - `/Users/haruca/work/rust/surtr/crates/eldr/src/builtin.rs`
  - 必要なら `/Users/haruca/work/rust/surtr/crates/eldr/src/error.rs`
- 受け入れ条件:
  - `vm.rs` の未カバー正常系 opcode が unit test で直接検証される
  - `ListHead` / `ListTail` / `GetField` / `GetTag` などの error path が unit test で固定される
  - `builtin.rs` の型不一致 / 範囲超過 / `eprint` 分岐が unit test で固定される
  - runtime error 表示と verbose 環境変数パースのテストが追加される
- テストケース:
  - `cargo test -p eldr`

### Commit 10: `docs: align specs with generic types, builtin type declarations, and runtime policy`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/doc/float.md`
  - `/Users/haruca/work/rust/surtr/doc/要件定義v9.md`
  - `/Users/haruca/work/rust/surtr/doc/surtr_runtime_failure_policy.md`
  - `/Users/haruca/work/rust/surtr/doc/EldrVM_spec.md`
  - `/Users/haruca/work/rust/surtr/README.md`
  - `/Users/haruca/work/rust/surtr/AGENTS.md`
- 受け入れ条件:
  - `Float` の位置づけが「現行実装は維持、厳密仕様は別紙で継続策定」に揃う
  - `Int=BigInt` と内部 ID 分離が正本ドキュメントに反映される
  - `AstTy::Generic`、match arm LHS、single quote escape 対称化、closure param annotation 任意化、fuel 方針が文書に反映される
  - README と AGENTS の説明が現状コードと矛盾しない
- テストケース:
  - ドキュメント変更のみのため自動テストなし

### Commit 11: `tests: add pending coverage for deferred parser/type/runtime work`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/tests/integration/pending_regressions.rs`
  - 必要なら各 crate の ignored unit test
  - `/Users/haruca/work/rust/surtr/crates/rune/Cargo.toml`
  - `/Users/haruca/work/rust/surtr/doc/テスト方針.md`
  - `/Users/haruca/work/rust/surtr/doc/test_runner_design.md`
- 受け入れ条件:
  - 将来必要なテストケースが `#[ignore]` 付きで先置きされる
  - pending 理由がテスト名または ignore 理由で分かる
  - 既存 `cargo test` を壊さない
  - `doc/test_runner_design.md` にある未実装項目のうち、先に固定したいものが pending coverage に落ちている
  - 今回 Issue に送った `expected=None` closure 推論、OOM 詳細、fuel 実装本体の future coverage が先置きされる
- テストケース:
  - `cargo test -p rune`

### Commit 12: `spec: refile resolved decisions vs future open issues`

- 修正範囲:
  - `/Users/haruca/work/rust/surtr/doc/open-issues.md`
  - `/Users/haruca/work/rust/surtr/doc/surtr_stdmodv1_followup_tasks.md`
  - `/Users/haruca/work/rust/surtr/doc/surtr_stdmodv1_issue_reorg.md`
  - `/Users/haruca/work/rust/surtr/doc/surtr_builtin_type_resolution_design.md`
  - `/Users/haruca/work/rust/surtr/doc/surtr_operator_impl_memo.md`
- 受け入れ条件:
  - 「すでに要件定義へ取り込んだもの」と「将来実装」の境界が明確になる
  - `OI-001`〜`OI-004` は今回の決定内容に沿って更新され、未確定点だけが残る
  - `@@builtin` + `type` 宣言読み取りは「コード上の型宣言を受理し、builtin semantics は compiler が持つ」という境界で整理される
  - 演算子/Float/標準モジュール課題の重複記述が減る
  - `doc/open-issues.md` の非マクロ論点について、今回クローズ可能なものと継続課題が区別される
  - `doc/surtr_stdmodv1_followup_tasks.md` と `doc/surtr_stdmodv1_issue_reorg.md` の未実装項目が backlog として読める状態になる
- テストケース:
  - ドキュメント変更のみのため自動テストなし

## 先に置く pending テスト

- `@@builtin` 単独行 + `type` 宣言を parser が読めること
- `AstTy::Generic` 導入後に `Option<Int>` や `List<Int, String>` をサイレントに潰さないこと
- match arm の LHS が `SafeBind.LHS` と同じ grammar を通り、binding catch-all が exhaustive として扱われること
- closure param annotation が optional であり、期待型あり / なしの境界が固定されること
- `surtr test` が `@@builtin` と `@@test` を同時に含む標準 module を壊さないこと
- fuel 実装を入れるときに停止理由と表示がぶれないこと
- `Float` の `NaN` / `Infinity` / `-0.0` 契約が確定したら、その挙動を fixture 化すること

## 今回の完了条件

- `作業フロー.md` が基準文書として成立している
- 修正ポイントが「今回確定」「確認不要でタスク化」「Issueへ退避」に整理されている
- `Int=BigInt` と内部 ID 分離の基盤がコードに反映されている
- 仕様正本が `Float`、`AstTy::Generic`、match arm LHS、`@@builtin + type`、runtime policy の現状に合わせて整理されている
- 将来実装分のテストが pending として先置きされている
