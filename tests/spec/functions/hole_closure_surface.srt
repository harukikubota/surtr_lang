always: (_ -> Int) = const(1)
print(to_string(always("ignored")))

print(to_string(id(7)))

def make() -> (_ -> Int) {
  const(2)
}

next = make()
print(to_string(next(False)))

idle: (-> Unit) = noop()
print(inspect(idle()))
