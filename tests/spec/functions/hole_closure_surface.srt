keep_one: (_ -> Int) = always(1)
print(to_string(keep_one("ignored")))

print(to_string(id(7)))

def make() -> (_ -> Int) {
  always(2)
}

next = make()
print(to_string(next(False)))

idle: (-> Unit) = noop()
print(inspect(idle()))
