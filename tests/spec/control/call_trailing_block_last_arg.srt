def take(flag: Boolean, block: (-> Int)) -> Int {
  if(flag, block(), 0)
}

print(to_string(take(True) { num = 10; num }))

def run_if(flag: Boolean, block: (-> Unit)) -> Unit {
  if_then(flag, block())
}

v = run_if(True) { print("x") }
print(to_string(v))
