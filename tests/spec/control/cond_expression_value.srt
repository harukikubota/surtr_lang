print(to_string(cond {
  False => 0,
  1 < 2 => 1,
  True => 2,
}))
print(to_string(cond {
  False => 0,
  False => 1,
  True => fallback_value(),
}))

def fallback_value() -> Int {
  print("fallback")
  2
}
