defagent Env {
  meta {
    instance: Singleton
    init_policy: Eager
    state: String
  }

  @init
  def init() -> Result<String> {
    Ok("surtr-home")
  }

  @get
  def get(state: String, key: String) -> Result<String> {
    Ok(key ++ "=" ++ state)
  }
}
