def trim_name(user: User) -> String {
  user.name
}

user = User("alice", 30)
rename = &User::rename_to("bob")
print(user |> &User::get_name)
print(user |> User::get_name())
print(to_string(rename(user)))
print(user |> trim_name())
