defstruct User {
  name: String,
  private password: String,
}

impl User {
  def new(name: String, password: String) -> Self {
    User { name: name, password: password }
  }

  def reveal_password(self) -> String {
    self.password
  }
}
