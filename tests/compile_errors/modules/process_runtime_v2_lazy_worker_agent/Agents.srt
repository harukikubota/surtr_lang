defagent Counter {
  meta {
    instance: Worker
    init_policy: Lazy
    state: Int
  }

  @init
  def init() -> Result<Int> {
    Ok(0)
  }

  @get
  def get(state: Int) -> Result<Int> {
    Ok(state)
  }

  @set
  def set(_state: Int, next: Int) -> Result<Int> {
    Ok(next)
  }
}
