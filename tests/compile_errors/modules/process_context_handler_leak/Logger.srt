defagent Logger {
  meta {
    instance: Singleton
    init_policy: Eager
    state: Int
    handlers {
      out: OutHandler = StdOut
    }
  }

  @init
  def init() -> Result<Int> {
    Ok(0)
  }

  @get
  def get(state: Int) -> Result<PID<OutHandler>> {
    Ok(ctx.out)
  }
}
