defenum Boxed<$T> {
  Box($T),
}

impl From<Option<$T>> for Boxed<$T> {
  def from::<Option<$T>>(self: Self) -> Option<$T> {
    match self {
      Boxed::Box(value) => Option::Some(value),
    }
  }
}
