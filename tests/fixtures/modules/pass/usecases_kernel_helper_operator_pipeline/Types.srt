defstruct User {
  name: String,
  age: Int,
  active: Boolean,
}

impl User {
  def new(name: String, age: Int, active: Boolean) -> Self {
    User { name: name, age: age, active: active }
  }
}

deferror HiddenUser {
  "hidden user"
}
