defstruct Vec2 {
  x: Int,
  y: Int,
}

impl Vec2 {
  def new(x: Int, y: Int) -> Self {
    Vec2 { x: x, y: y }
  }
}

impl Add for Vec2 {
  def add::<Vec2>(self: Self, rhs: Self) -> Self {
    Vec2 { x: self.x + rhs.x, y: self.y + rhs.y }
  }
}
