deftrait Render {
  def render(value: $A, other: $C) -> String
  where
    $A: Eq
    $C: Show
}

impl Render for Int {
  def render(value: $B, other: $D) -> String
  where
    $D: Show
    $B: Eq
  {
    if(Eq::eq(value, value), Show::to_string(other), Show::to_string(other))
  }
}

print("ok")
