# Surtr File Icons

Surtr source (`.srt`) と Eldr bytecode (`.eldr`) 用の file icon 拡張です。

- `.srt` と `.eldr` に Surtr icon を割り当てる
- 単体の icon theme として利用できる
- `vscode-icons` の custom icon assets としても再利用できる

## 開発

```bash
cd vscode-extensions
npm install
cd surtr-file-icons
npm run package
```
